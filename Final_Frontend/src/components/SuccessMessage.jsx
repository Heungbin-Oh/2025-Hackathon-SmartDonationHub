// Currently not used - in case we want to make fancier success message
import { Alert } from "react-bootstrap";

export default function SuccessMessage() {
  return <Alert variant="success" className="text-center">Thank you for your donation!</Alert>;
}
